#include <stdio.h>
#include <time.h>

int main() {
    time_t t;
    struct tm *timeinfo;
    char buffer[20];

    time(&t);
    timeinfo = localtime(&t);
    strftime(buffer, sizeof(buffer), "%I:%M:%S %p", timeinfo);
    printf("Current Time: %s\n", buffer);

    return 0;
}
