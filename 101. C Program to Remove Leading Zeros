#include <stdio.h>
#include <string.h>

int main() {
    char str[100];
    printf("Enter number string: ");
    scanf("%s", str);

    int i = 0;
    while (str[i] == '0') i++;

    if (str[i] == '\0') printf("0\n");
    else printf("Without leading zeros: %s\n", str + i);

    return 0;
}
