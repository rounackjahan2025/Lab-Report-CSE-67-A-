#include <stdio.h>

int main() {
    double num;
    char str[50];
    printf("Enter a double: ");
    scanf("%lf", &num);
    sprintf(str, "%lf", num);
    printf("String: %s\n", str);
    return 0;
}
